- Symptom: clicking Generate in the extension Step 2 scraped-technology review could leave the user staring at thinking dots, then show duplicated example content if a second generate request raced the first.
- Root cause: the Generate path streamed partial assistant content into the persisted interview conversation while the thinking indicator only checked the separate pending-assistant message slot. Rapid duplicate requests could also append another final examples message because the API did not treat generated examples as idempotent.
- Fix: stream Generate output through the same pending assistant message used by answer streaming, clear it when the final server payload arrives, guard the Generate click with an immediate in-flight ref, start a tiny model-streamed opening question before the heavier examples-card stream, and have the API return the existing interview when examples already exist instead of appending another examples message. The extension sends technology names from both the interview summary and visible technology cards so the early opening stream does not fall back to the slow profile-read path.
- Latency note: bearer auth for the extension uses a short-lived hashed session cache under `.job-helper-data/api-session-cache` so Step 2 Generate can open the NDJSON stream without a slow repeated session lookup. The visible opening words still come from OpenAI streaming, not client-side replay.
- Guardrail: every Step 2 streamed assistant turn should use one transient pending-message surface until the final persisted conversation arrives. Server actions that add model-generated chat messages should be idempotent against duplicate client requests.
