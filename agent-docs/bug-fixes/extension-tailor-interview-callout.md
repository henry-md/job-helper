- Symptom: the Chrome extension could look stuck on Tailor Resume Step 1 even when the backend had already prepared a follow-up question, because the visible run card stopped at `Clarify missing details` while the actual chat composer sat farther down the side panel.
- Root cause: the interview UI existed, but it was surfaced as a separate card below the run summary without a strong in-card cue or a reliable reveal action, so the waiting-for-input state could read like passive loading instead of "your turn."
- Fix: show an in-card follow-up callout with the latest assistant question preview plus an explicit `Open chat` action that reveals and focuses the interview card on demand instead of auto-opening it.
- Guardrail: when a multi-step workflow pauses for user input, the currently visible progress surface should either host the input directly or provide an unmistakable, deliberate affordance that reveals the required response UI without stealing focus unexpectedly.
