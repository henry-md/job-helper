- Symptom: the saved tailored-resume overflow menu in the extension opened inside the list scroll container, so the popover got clipped and forced awkward scrolling instead of floating above nearby rows.
- Root cause: the saved-row menu was rendered as an absolutely positioned child of the scrollable tailored-resume list, so the list's overflow clipping won even though the popover itself was visually styled like a free-floating menu.
- Fix: render the saved-row popover as a fixed-position floating overlay anchored to the row action trigger, and keep outside-click handling aware of both the trigger shell and the floating popover.
- Follow-up: even fixed-position row menus need viewport-edge clamping, not just scroll-container escape. Measure the mounted menu when possible, use a conservative fallback height before first paint, clamp both left and top against the viewport, and keep a `max-height: calc(100vh - padding)` overflow backstop so bottom-row actions never leave the page.
- Symptom: after confirming a saved tailored-resume delete, the row sat in place until the server delete and full personal-info refresh finished.
- Root cause: the extension waited for the delete request and the follow-up `/api/tailor-resume` + `/api/job-applications` refresh before removing the row from local state.
- Fix: optimistically remove the impacted tailored resumes and linked applications from the extension's in-memory personal-info snapshot as soon as delete is confirmed, then reconcile from server truth in the background and restore the prior snapshot only if the delete request fails.
